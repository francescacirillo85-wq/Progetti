# Progetti

1 - Machine learning training

Progetto di ML training usando la regressione logistica, dataset simulato con 800 unità per il training set e 200 per il validation set

2 - Slot machine simulata

E' stato simulato il funzionamento di una slot machine con variazioni sui parametri 'probabilità di vittoria', 'budget iniziale', 'numero vittorie | target-budget'
